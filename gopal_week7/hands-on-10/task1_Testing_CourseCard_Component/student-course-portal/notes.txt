HANDS-ON 10 — Task 1: Unit Testing CourseCardComponent
=======================================================

INSTALL & RUN:
  npm install
  ng test                     — runs Karma in watch mode (opens Chrome)
  ng test --code-coverage     — generates coverage/ folder with HTML report

WHAT WAS BUILT
--------------
components/course-card/course-card.ts
  — @Input() course: Course
  — @Input() isEnrolled: boolean
  — @Output() enrollRequested: EventEmitter<number>  (emits course.id)
  — ngOnChanges: logs course name change to console

components/course-card/course-card.spec.ts   ← THE SPEC FILE
  Step 101: beforeEach — TestBed.configureTestingModule({ imports: [CourseCard] })
            standalone component uses imports[], not declarations[]
  Step 102: 'should create' — expect(component).toBeTruthy()
  Step 103: '@Input rendering' — set course, detectChanges(), query By.css('h3')
  Step 104: '@Output' — spyOn(enrollRequested, 'emit'), click button, toHaveBeenCalledWith(1)
  Step 105: 'ngOnChanges' — spyOn(console, 'log'), call ngOnChanges manually, assert log message

KEY JASMINE CONCEPTS
--------------------
describe()          — test suite block
it()                — individual test case
expect(x).toBe(y)   — strict equality
toBeTruthy()        — truthy check (component created)
toHaveBeenCalledWith(arg) — spy call assertion
spyOn(obj, 'method')      — replace method with spy
fixture.detectChanges()   — run Angular change detection
By.css('.selector')       — query scoped to component DOM
SimpleChange(prev, curr, isFirst) — manual ngOnChanges payload

TEST COUNT: 9 specs in CourseCard describe block
